- name: variable preference
  host: localhost
  vars:
    WHISHES: "Morning from  PLAY variable"
    vars_files:
      - vars.yaml
    vars_prompt:
      - name: WISHES
        prompt: Please Enter WISHES
        private: false
      tasks:
        - name: Check preference
          vars:
            WISHES: "Morning from TASK variable"
          ansible.builtin.debug:
            msg: "Hello, Good {{WISHES}}"  